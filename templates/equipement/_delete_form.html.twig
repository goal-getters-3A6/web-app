<form method="post" action="{{ path('app_equipement_delete', {'idEq': equipement.idEq}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipement.idEq) }}">
    <button class="btn">Supprimer</button>
</form>

