{% extends 'base-clean.html.twig' %}
{% block body %}
<br><br><br><br><br><br><br>
	<section class="login spad">
		<div class="container">
			<div class="row">
				<div class="col-lg-6">

					<div class="login__form">
						{% if not form.vars.valid %}
							{% for child in  form.children %}
								{% for error in child.vars.errors %}
									<div class="alert alert-danger">{{ error.message }}</div>
								{% endfor %}
							{% endfor %}
						{% endif %}
						{{ form_start(form ,  {'attr' : {'novalidate' : 'novalidate'}}) }}

						{{ form_end(form) }}
					</div>
				</div>
				<div class="col-lg-4 flex-column m-3">
					<div style="display: grid" class="login__register ">
						<a class="primary-btn" style="border: none; background-color:blue; color:white;" href="{{ path('edit_profile_password') }}">Edit Password</a>
						<a class="primary-btn" style="border: none; background-color:blue; color:white;" href="{{ path('setup_tfa') }}">Activate 2FA</a>
						<form method="post" action="{{ path('delete_profile') }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
							<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
							<button style="border: none;  background-color:red; color:white;" class="primary-btn">Delete Account</button>

						</form>
					</div>
					<a class="primary-btn" style="border: none; background-color:gray; color:white;" href="{{ path('profile') }}">Back</a>


				</div>
			</div>
		</div>
	</section>
{% endblock %}
