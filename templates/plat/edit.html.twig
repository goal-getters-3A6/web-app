{% extends 'plat/alimentaireback.html.twig' %}

{% block title %}Edit Plat{% endblock %}

{% block body %}
<div class="container-fluid  style="height: 100vh;">
    <div class="card shadow">
    <div style="height: 100px;"></div>
      
        <div class="card-header bg-primary text-white">
            Edit Plat
        </div>
        <div class="card-body">
            {{ include('plat/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_plat_indexb') }}" class="btn btn-primary">Retour a la liste</a>
            {{ include('plat/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}